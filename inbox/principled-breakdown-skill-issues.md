# Principled Breakdown Skill 问题清单

审查日期：2026-05-09（初查）、2026-05-10（复查）

审查对象：`skills/principled-breakdown/SKILL.md`、`skills/principled-breakdown/agents/openai.yaml`

---

## 第一轮审查（2026-05-09）

### 1. ~~缺少"不适用场景"~~ ✅ 已修复

~~**严重程度**：高~~

> 已新增"不适用或简化使用场景"章节，定义了不适用/简化/正常/完整四档。

### 2. ~~输出模板缺少规模对照~~ ✅ 已修复

~~**严重程度**：高~~

> 已按小型/中型/大型三种规模给出三套不同模板。

### 3. ~~苏格拉底提问库存在冗余~~ ✅ 已修复

~~**严重程度**：中~~

> 问题已按"澄清目标 / 澄清边界 / 澄清验收"分组，删除了重叠项。

### 4. ~~缺少 walkthrough example~~ ✅ 已修复

~~**严重程度**：中~~

> 已新增"备份工具"的 walkthrough 示例。

### 5. `agents/openai.yaml` 价值存疑

**严重程度**：低（未处理）

该文件仅包含一个 `default_prompt`，本质上是 SKILL.md 的一句话概括，且只在 OpenAI 环境的 agent 中加载。在当前仓库只面向 Claude Code 的场景下，该文件不起作用。

建议：
- 如果确实需要跨平台支持，应包含完整的 agent 配置（system prompt、tools、parameters），而不只是 default_prompt。
- 如果只面向 Claude Code，可删除此文件，避免维护负担。

---

## 第二轮审查（2026-05-10）

### 6. 规模命名不一致

**严重程度**：中

"不适用或简化使用场景"章节使用了 `不适用 / 简化使用 / 正常使用 / 完整使用` 四档命名，但输出模板章节用的是 `小型任务 / 中型任务 / 大型任务`。两套命名没有对应关系说明，agent 看到"简化使用"后往下找模板时找不到匹配名称。

建议：
- 统一为一套命名，或在场景章节直接用括号注明对应模板，如"简化使用（对应小型任务模板）"。

### 7. "不适用"触发条件有一项模糊

**严重程度**：低

> 用户只要一个明确事实、翻译、格式转换、命令输出

"命令输出"含义不清。如果用户说"帮我跑 `git log` 看看"，这类纯信息查询 skill 本来就不会触发，不需要在排除列表中声明。

建议改为"纯信息查询或单步命令执行"，或者直接删除"命令输出"四个字。

### 8. Walkthrough 没有和模板格式对齐

**严重程度**：低

当前 walkthrough 是叙述式的一段话，而 skill 的核心产出是三套模板。如果 walkthrough 中至少有一段用实际模板格式展示填充后的效果（比如把备份工具用小型模板输出一遍），agent 对"如何填充模板"的理解会更准确。

建议在 walkthrough 末尾追加一段以实际模板格式呈现的输出示例。
